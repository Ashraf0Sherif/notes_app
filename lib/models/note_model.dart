class NoteModel {
  NoteModel(
      {required this.title,
      required this.subTitle,
      required this.date,
      required this.color});

  final String title, subTitle, date;
  final int color;
}
